name: Publish Package

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version to publish (leave empty to use package.json version)"
        required: false
        type: string
      tag:
        description: "npm dist-tag (latest, beta, next, etc.)"
        required: true
        default: "latest"
        type: choice
        options:
          - latest
          - beta
          - next
          - alpha

jobs:
  publish:
    name: Publish to npm & GitHub Packages
    runs-on: ubuntu-latest

    permissions:
      contents: write
      id-token: write # Required for npm provenance
      packages: write # Required for GitHub Packages

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"
          cache: "pnpm"
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate version
        run: |
          CURRENT_VERSION=$(node -p "require('./libs/mat-datatable/package.json').version")
          if [ -z "${{ inputs.version }}" ]; then
            echo "No version provided, will use current version: $CURRENT_VERSION"
            echo "⚠️  Warning: Publishing existing version. Make sure this version hasn't been published yet!"
            echo "VERSION=$CURRENT_VERSION" >> $GITHUB_ENV
          else
            echo "Will bump version to: ${{ inputs.version }}"
            echo "VERSION=${{ inputs.version }}" >> $GITHUB_ENV
          fi

      - name: Bump version in library package.json
        if: inputs.version != ''
        run: |
          cd libs/mat-datatable
          npm version ${{ inputs.version }} --no-git-tag-version
          echo "✅ Updated library version to ${{ inputs.version }}"

      - name: Commit version bump
        if: inputs.version != ''
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add libs/mat-datatable/package.json
          git commit -m "chore: bump version to ${{ inputs.version }}"
          git push origin HEAD:${{ github.ref_name }}

      - name: Run linting
        run: pnpm lint

      - name: Run tests
        run: pnpm test

      - name: Build library
        run: pnpm build:lib

      - name: Publish to npm
        working-directory: dist/mat-datatable
        run: |
          if [ "${{ inputs.tag }}" = "latest" ]; then
            npm publish --access public --provenance
          else
            npm publish --access public --tag ${{ inputs.tag }} --provenance
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Setup Node.js for GitHub Packages
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"
          registry-url: "https://npm.pkg.github.com"
          scope: "@acheddir"

      - name: Publish to GitHub Packages
        working-directory: dist/mat-datatable
        run: |
          if [ "${{ inputs.tag }}" = "latest" ]; then
            npm publish --access public
          else
            npm publish --access public --tag ${{ inputs.tag }}
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Git tag
        if: inputs.version != ''
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a "v${{ inputs.version }}" -m "Release v${{ inputs.version }}"
          git push origin "v${{ inputs.version }}"

      - name: Create GitHub Release
        if: inputs.version != ''
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v${{ inputs.version }}
          release_name: Release v${{ inputs.version }}
          body: |
            ## @acheddir/mat-datatable v${{ inputs.version }}

            Published to npm and GitHub Packages with tag: ${{ inputs.tag }}

            ### Installation

            **From npm:**
            ```bash
            npm install @acheddir/mat-datatable@${{ inputs.version }}
            ```

            **From GitHub Packages:**
            ```bash
            npm install @acheddir/mat-datatable@${{ inputs.version }} --registry=https://npm.pkg.github.com
            ```

            ### Changes
            See [CHANGELOG.md](https://github.com/acheddir/mat-datatable/blob/main/CHANGELOG.md) for detailed changes.
          draft: false
          prerelease: ${{ inputs.tag != 'latest' }}

  summary:
    name: Publish Summary
    runs-on: ubuntu-latest
    needs: publish
    if: always()

    steps:
      - name: Check publish status
        run: |
          if [ "${{ needs.publish.result }}" != "success" ]; then
            echo "❌ Package publish failed"
            echo ""
            echo "💡 Common issues:"
            echo "  - Trying to publish an already published version"
            echo "  - Make sure to provide a new version number when running the workflow"
            echo "  - Check npm authentication token"
            exit 1
          else
            echo "✅ Successfully published to npm and GitHub Packages"
            echo "📦 Package: @acheddir/mat-datatable"
            if [ -n "${{ inputs.version }}" ]; then
              echo "📌 Version: ${{ inputs.version }}"
            fi
            echo "🏷️  Tag: ${{ inputs.tag }}"
            echo ""
            echo "Available at:"
            echo "  - npm: https://www.npmjs.com/package/@acheddir/mat-datatable"
            echo "  - GitHub: https://github.com/acheddir/mat-datatable/pkgs/npm/mat-datatable"
          fi
